# Передача пакета SkySend от 24.09.2026

Полный пакет WordPress после второй части правок от 24.09.2026 собран с
тестовой ВМ и проверен восстановлением в отдельную временную БД. На
`gw1.isg.dev` и в jail `www` ничего не устанавливалось.

| Параметр | Значение |
| --- | --- |
| Архив | `skysend-wordpress-20260924_114213.zip` |
| Размер | 56 124 591 байт |
| SHA-256 | `6b867648a6263791ef286cdf9e83f6b6abdb382fb6f440bd8e075cd9b77e8d30` |
| Снимок | 24.09.2026, 11:42:30 UTC |
| Исходники Git | `d5d4dd6` |
| Файлов в ZIP | 5 327 |
| Блоков главной | 602, без невалидных |
| Вложений медиатеки | 155 |

Проверены контрольные суммы ядра WordPress 7.1.1 и двух плагинов, все 18
проверок клона, импорт SQL в пустую БД, 15 HTTP-проверок главной/SEO/карт
сайта и доступность локальных ресурсов. После скачивания архива SHA-256
повторно совпала.

Этот снимок включает официальный кадр загрузки FastSYS, исправленный баннер
с напольным платёжным терминалом, девять категорий и 108 примеров из старого
каталога SkySend с локальными логотипами и листанием, обновлённые загрузки и
иконки соцсетей. В слайде FastSYS нет фразы о работе каждого устройства
«на протяжении десятилетий»: официальные материалы не подтверждают такое
обещание. Для каталога также указано, что актуальный состав услуг уточняется
при подключении.

Изменения страницы 67, шапки 68, футера 69, Global Styles 66 и site icon
хранятся в **БД**. Поэтому **обновление базы обязательно**: копирование только
файлов или только `uploads` не покажет новые секции, ссылки и favicon. Архив
содержит полный `database.sql`, но это **не SQL-патч**. Его нельзя импортировать
поверх существующей БД с таблицами. Безопасная установка: новый каталог,
новая пустая БД, импорт полного дампа, `wp search-replace` на фактический домен,
проверка, переключение vhost. Старые каталог и БД оставить для отката.

Пошаговые команды — [README-INSTALL](../deploy/wordpress/README-INSTALL.md),
тот же файл находится внутри ZIP. Если обновляется текущий стенд
`new.skysend.ru`, переменная `NEW_URL` должна быть `https://new.skysend.ru`;
для окончательного домена — его реальный HTTPS-адрес. Не использовать
`robots-production.txt` на промежуточном стенде.

Пакет содержит SQL и историю сайта, его передавать только закрытым каналом,
не выкладывать на GitHub, в FTP-листинг или webroot. Учётные пароли в копии
рандомизированы; после установки администратор задаёт новый пароль WordPress.
