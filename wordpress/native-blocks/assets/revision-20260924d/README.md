# Image edits, 24 September 2026

Both PNG masters were edited with the built-in image-generation tool from
existing SkySend illustrations. `prepare-images.mjs` makes the optimized WebP
files used by WordPress. The previous media attachments remain intact.

Final image-edit prompts:

- Finance banner: “Change only the two-line callout in the SkySend finance
  illustration. Place exact `Доход` and `+20%` together immediately left of the
  upright payment terminal, fully visible in the rightmost 3:2 website crop.
  Keep the terminal, money bag, coins, background and flat blue style; no other
  text, objects, borders or shadows.”
- XML gateway card: “Change only the upper-left text of the original SkySend
  gateway-partner image to exact uppercase Latin `XML`, medium-dark navy and
  legible at thumbnail size. Preserve the woman, pose, headset, hand, circular
  interface, background and photographic composition.”
